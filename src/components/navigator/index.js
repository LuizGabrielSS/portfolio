import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Navigator(){

    return(
        <Box>
            <Typography
                sx={{
                    color: "text.top"
                }}
            >
                Teste
            </Typography>
        </Box>
    )

}